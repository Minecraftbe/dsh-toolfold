# AGENTS.md

> 通用约定放这里；历次结对的实战沉淀（根因、修法、坑位）在 **`EXPERIENCE.md`**。
> 动手前先读其中与本次改动相关的章节，两个文件都读完再动手。

## 🔴 红线区（最高优先级，违反即事故）

> 以下 5 条是死刑条款：不解释，不引申，不打折。单列于此，不与普通条目混排。
> 拿不准就停下来问。执行细节见对应章节，但允许与否只看这里。

- **R1 · 未获允许，一字不得落**：`AGENTS.md` / `EXPERIENCE.md` 的任何增删改，
  写入前必须经维护者审阅原文并明确允许。提议、还价、顺手都不产生写入权，
  先写后滚同样是先斩后奏。（执行流程见 §5）
- **R2 · 不动版本号**：`package.json version` 只有发版时才动，功能分支上一律不动。
- **R3 · 提交要自觉，推送要指令**：`vibe/*` 分支下，每完成一个完整改动即自动
  `commit`（仅本地，不 `push`），除非维护者明确说"不要提交"；
  任何 `push`，以及对 `main` 的一切操作（合入、推送），永远等维护者明确指令。
  commit 信息用英文 conventional commits 风格。
- **R4 · 不在 `main` 上直接改**：功能走 `vibe/*` 分支，`main` 只接受合入。
- **R5 · 只改 `src/`**：`lib/` 是 `tsdown` 生成物（gitignore），不直接碰；
  流程永远是改 `src` → `pnpm build` → 验证。

## 1. 工作流

1. **动手前先详细描述方案**：把根因、改法、验证步骤讲清楚，等维护者点头再动手。
2. **先复现，后修复**：修 bug 先在本地复现现象（无头探针、最小用例均可），确认根因再改；
   新逻辑配回归测试，先让测试变红，再让它变绿。
3. **断言以官方产物源码为准，不许猜**：结论必须能指到 `store` 中的具体文件行号；
   拿不准就写探针验证，别凭印象编 API。
4. **一次只做一件事**：一个分支一个主题，不夹带无关重构。
5. **保持线性历史**：`pull` 一律 `--rebase`；合入前先 rebase 到目标分支再 fast-forward，
   不造 merge commit（禁用 `--no-ff`）。

> 版本、提交、分支、产物四条是红线（R2–R5），见顶部红线区，这里不再重复。

## 2. 验证门槛（本地能跑的必须全绿）

```sh
pnpm build          # 必须干净
pnpm test:engine    # 折叠引擎 + 卡片冒烟，看到 ALL CHECKS PASSED
pnpm test:version   # 版本判定矩阵，看到 ALL VERSION CHECKS PASSED
```

维护者侧的最终验证永远是：`pnpm install:dsh` → 重启 DSH（25566）→ 刷新页面看真机效果。
注意 `install:dsh` 装的是构建产物，**不 `pnpm build` 就重装等于没改**。

## 3. 通用准则

1. **单一真相源**：同一份事实只存一处，其余地方引用或构建期派生，禁止手抄多份。
   （区间、文案、token 皆如此；README 只写指向，不抄数字。）
2. **能用官方底座就不用手造**：seed 表模块、transport、slot、theme token 优先；
   卡片 chrome 这类官方明示归插件的部分，老实自己画，别套第一方内部组件。
3. **运行时依赖必须可降级**：构建期用 `neverBundle` 保持 external，
   运行时懒加载 + try/catch + fallback，任何环境下模块加载都不许炸。
4. **选语义对得上的机制**：警告用警告语义的字段，拦截用拦截语义的；
   别拿重武器表达轻意思，反之亦然。
5. **静默失败优于误报**：判断不了就报 `unknown`，不打扰用户；
   绝不为了"看起来工作了"而猜一个结论。

## 4. 文档规范

- `README.md` / `README.en.md`：只写指向性描述，不抄会过期的具体数字。
- `CHANGELOG.md`：未发版改动记在 `## [Unreleased]` 下，分缺陷修复、功能变更、工程变更三节。
- 实战沉淀统一进 `EXPERIENCE.md`（扁平章节加坑位表），不散落在各处注释里。
- 代码注释只写"为什么这样"，不写"做过什么"（那是 CHANGELOG 的活）。

## 5. 文档自我扩充规则

> 本节是红线 **R1** 的执行流程：允许与否以顶部红线区为准，这里只讲怎么办。

### 5.1 允许分级：指示只能立项，不能代替过审

| 级别 | 情形 | 流程 |
|---|---|---|
| **L1 · 逐字允许（默认）** | Agent 自发提议、日常改动附带、任何看着顺手的扩充 | Agent 给出文件、章节、拟写原文 → 维护者逐字过审 → 允许后写入 |
| **L2 · 指示即立项（例外）** | 维护者明确指示"把 X 记下来" | 指示等于"该记"的决策已批，省掉立项论证；但 AI 必须先把 X 细化成文件、章节、原文，**仍走 L1 过审，允许后才写**。先写后滚同样是先斩后奏 |
| **L3 · 不存在** | 默认没有任何长期或默示允许 | 不得自行假设"这类小改以后可以直接写"；维护者如给出长期允许，以其原话为准，不得引申 |

判定标尺只有一条：**落字之前，维护者是否看过要落字的原文**。
L2 省掉的只是"要不要记"，省不掉"怎么写"：指示越具体，细化越快；
指示越模糊，AI 越要多做细化功课，但**细化完一律回 L1**，没有第二条路。

- ✅ L2 如：「把"先复现后修复"记到 `AGENTS.md` 工作流」——方向已批，
  AI 写出拟入条文贴出来，过审后写入。不用论证该不该记，但怎么写照样审。
- ❌ 脑补落字如：「把今天讨论的东西记一下」——AI 自行整理、自行落字，
  全程没给维护者看过原文。正确做法是整理出草稿，走 L1。

### 5.2 AI 的还价权：专家建议，不是决定权

前提：vibe 模式下维护者未必有足够的代码与项目经验，未必判断得出"该不该记"。
记多了文档腐烂、红线贬值；记少了后人再踩。这个质量关，AI 必须用专业判断来把：

1. **应当还价的情形**：X 与现有条目重复；X 过于琐碎或一次性，不值得进文档；
   X 放错了地方（经验塞进红线，或红线沉到经验里）；X 表述模糊，需要澄清；
   X 与现有条目冲突。反向也成立：维护者说"不用记"，但 AI 判断后人必踩，
   允许且**应当**谏言一次。
2. **还价的形式**：摆理由，给替代方案（改写、换文件换章节、降级为代码注释、不记），
   把选择题抛回给维护者，而不是只说"不行"。
3. **边界（红线）**：还价永远只是建议环节。**最终该不该记、记成什么样，
   决定权只在维护者手上**。拍板之后一律回到 L1（拟文 → 过审 → 写入）。
   不得以"我觉得不该记"为由拖延、打折或拒绝执行已批准的内容；
   真觉得有问题就继续摆理由说服，但手上按批准的办。
4. 还价不产生任何写入权：哪怕双方达成一致，落字前仍要过 L1。
   例外只有 L2 一种，且 L2 里同样没有先斩后奏的位置。

### 5.3 提议与日常

1. **Agent 有主动维护文档的义务**：工作中发现两份文档缺了某条"后人会再踩"的
   红线或经验时，**应当主动提出扩充建议**，不必等维护者开口。
   等维护者想起来才补，等于让下一个人再踩一遍。但义务的边界止于"提议"，
   永远到不了"直接写"。
2. **提议必须具体**：文件、章节、拟写原文三件套一次给齐，
   让维护者能逐字过审，而不是对着一个模糊的方向点头。
3. 日常改代码不顺手改文档；与本次改动直接相关的文档更新，随改动一起提出、一起过审，
   同样先审后写（L1）。
