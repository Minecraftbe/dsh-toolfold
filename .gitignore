# dependencies / 依赖
node_modules/

# generated artifacts — `prepare: pnpm build` recreates the whole lib/
# (npm publish and git-hosted installs both run prepare before packing).
# Sources live in src/client/ + src/host/; nothing under lib/ is tracked.
lib/

# logs
*.log

# OS
.DS_Store
Thumbs.db
diff.txt
